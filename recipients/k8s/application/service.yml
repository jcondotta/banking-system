apiVersion: v1
kind: Service
metadata:
  name: recipients
  namespace: prod
  labels:
    app: recipients
spec:
  selector:
    app: recipients
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
