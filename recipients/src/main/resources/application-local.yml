server:
  port: 8081

app:
  kafka:
    topics:
      recipient-created:
        topic-name: recipients-created
      recipient-deleted:
        topic-name: recipients-deleted

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/recipients_db?connectTimeout=3&socketTimeout=5
    username: admin
    password: password

  jpa:
    show-sql: false

management:
  data-prepper:
    logging:
      url: http://localhost:2021/log/ingest/

  otlp:
    metrics:
      export:
        enabled: false
