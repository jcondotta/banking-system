app:
  environment: test

cloud:
  aws:
    region:
      static: us-east-1
    credentials:
      access-key-id: test
      secret-key: test
    dynamodb:
      endpoint: ${AWS_DYNAMODB_ENDPOINT}
      tables:
        account-recipients:
          table-name: 'account-recipients'
