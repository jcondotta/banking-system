server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/ledger_db?connectTimeout=3&socketTimeout=5
    username: admin
    password: password

  jpa:
    show-sql: false

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

app:
  kafka:
    topics:
      bank-account-activated: bank-account-activated
      internal-transfer-requested: internal-transfer-requested
